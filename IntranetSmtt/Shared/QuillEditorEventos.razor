@inject IJSRuntime JS


<div id="editoreventos" class="h-40"></div>

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await JS.InvokeVoidAsync("quillIndexedDBEventos.init"); 
        }
    }
}

